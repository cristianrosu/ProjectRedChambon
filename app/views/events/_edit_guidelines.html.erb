Guidelines...

sun de acord ca bla bla blah


<% if !edit_mode %>
	<%= simple_form_for(@event, :html => {:class => 'form-events', :multipart => true}) do |f| %>
	        <%= f.button :submit, class:"btn btn-large btn-blue"%>
	<% end %>
<% end %>